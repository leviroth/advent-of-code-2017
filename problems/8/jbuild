(jbuild_version 1)

(executables
 ((names        (part_1 part_2))
  (preprocess (pps (ppx_jane)))
  (flags (:standard -short-paths))
  (libraries (base stdio))))

(ocamllex (lexer))

(menhir ((modules (parser))))
